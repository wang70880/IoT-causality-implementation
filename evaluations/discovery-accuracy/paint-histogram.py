import numpy as np 
from matplotlib import pyplot as plt

def draw_pcmci_accuracy():
    """ The x-axis should be the optional parameter: pc_alpha and alpha_level
        For each x_axis, there are two columns, each of which represents precision or recall.z
        The y-axis should be the ratio %.
    """
    pass